# idgen

Generate identifiers: 'uuid', 'uuid 3', 'hex 32', 'slug'.

Pure Python standard library — no dependencies, no API keys.

## Usage

```bash
xelian run xelian/idgen
```

Then type into the REPL — one line in, one line out.
