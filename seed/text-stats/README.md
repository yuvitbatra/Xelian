# text-stats

Paste a line of text, get characters, words, unique words, and reading time.

Pure Python standard library — no dependencies, no API keys.

## Usage

```bash
xelian run xelian/text-stats
```

Then type into the REPL — one line in, one line out.
