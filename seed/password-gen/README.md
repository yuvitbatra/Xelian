# password-gen

Generate strong passwords and passphrases: '16', 'passphrase 4', 'pin 6'.

Pure Python standard library — no dependencies, no API keys.

## Usage

```bash
xelian run xelian/password-gen
```

Then type into the REPL — one line in, one line out.
