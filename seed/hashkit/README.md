# hashkit

Hash and encode text: 'sha256 hello', 'md5 x', 'b64 secret', 'unb64 c2VjcmV0'.

Pure Python standard library — no dependencies, no API keys.

## Usage

```bash
xelian run xelian/hashkit
```

Then type into the REPL — one line in, one line out.
