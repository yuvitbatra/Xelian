# unit-convert

Convert units from the chat line: '10 km in miles', '72 f in c', '512 mb in gb'.

Pure Python standard library — no dependencies, no API keys.

## Usage

```bash
xelian run xelian/unit-convert
```

Then type into the REPL — one line in, one line out.
