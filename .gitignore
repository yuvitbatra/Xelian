/target
.superpowers/
*.xelian
*.md

# Python (registry backend + SDK)
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets — real values only ever go in a local .env or the Render
# dashboard, never in git. .env.example (checked in) documents the keys.
.env
.env.*
!.env.example
